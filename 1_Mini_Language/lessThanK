class Main {
    void main() {
        int k;
        int n;
        // input
        write("Enter the value of k: ");
        read(k);

        // iteration
        n = 2;
        while (n < k) {
            if (n % 2 == 0) write(n); //print even            n = n + 1;
        }
    }
}
